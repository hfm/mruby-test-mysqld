language: c
addons:
  apt:
    packages:
      - rake
      - bison
      - git
      - gperf
      - libonig-dev
compiler:
  - gcc
  - clang
before_script:
  - sudo initctl stop mysql
env:
  - MRUBY_VERSION=1.2.0
  - MRUBY_VERSION=master
script:
  - rake test
